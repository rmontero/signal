# A-07 — Truthful preflight and pilot setup review

Task: A-07 / I-01. Owner: assigned Astra worker A-07, queued worker started by the coordinator. Report date: 2026-09-13 (checks completed after 16:05 UTC). Shared checkout: `/Users/robcr/Projects/signal`, branch `codex/signal-track-rob`. Assignment and ledger ownership remain with the coordinator; this is the scoped handoff report, not a second task ledger.

## Result and owned changes

**PASS — scoped implementation and focused checks. PARTIAL — release configuration; live acceptance NOT RUN.** The initial handoff edited only the following six owned files; the explicitly authorized routing correction is recorded below:

- [scripts/preflight.ts](../../scripts/preflight.ts): default scope now intersects the recognized core/optional lists with names supplied to `evaluatePreflight`. Absent names do not block local work. Supplied blank, whitespace, literal unset and recognized placeholder core values fail. Results/logs contain only names, counts and fixed statuses. Strict mode retains the complete 16-name checklist and the existing `--strict` / `SIGNAL_PREFLIGHT_STRICT=1` switches. Optional configuration does not enable a feature or block core checks. The old exported `CURRENT_*` constants remain only for import compatibility; they no longer determine scope.
- [tests/unit/preflight.test.ts](../../tests/unit/preflight.test.ts): 11 focused tests cover changing/default scope, absent and undefined names, inherited-property rejection, blanks/placeholders in both modes, value privacy, optional independence, the full strict list, CLI switches/exit codes, empty-scope messaging and the live-acceptance disclaimer. CLI subprocesses use synthetic environments and do not inherit developer credentials or load dotenv files.
- [docs/ONBOARDING.md](../ONBOARDING.md): permits current-scope local development, distinguishes passive observation from explicit action setup, and replaces the stale bootstrap-only handoff description with source-backed implementation context.
- [docs/INFRASTRUCTURE.md](../INFRASTRUCTURE.md): documents exact callback URLs, minimum passive permissions and conditional channel-metadata scopes, separate approved action grants, actual PostgreSQL pilot mapping/import behavior, current unsupported settings, web/task/operator environment separation, and configuration versus receipt/classification/release evidence.
- [docs/INTEGRATIONS.md](../INTEGRATIONS.md): removes conflicting live-status assertions in favor of acceptance requirements and the canonical ledger; includes the observer task, marks absent probe/optional task exports truthfully, and identified the OpenRouter routing gap resolved in the follow-up below.
- This report.

No branch change, agent spawn, package/lock/shared-contract/config/DB/Trigger edit, env-file edit, import, migration, login, provisioning, deployment, provider message/mutation, staging, commit or push occurred. Existing edits by other workers were preserved. Public official-document reads were the only remote checks; no private provider account state was inspected.

## Verification

All Node commands used `export PATH=/Users/robcr/.nvm/versions/node/v24.20.0/bin:$PATH`; `node --version` returned `v24.20.0`.

| Command/check | Result and evidence |
|---|---|
| `node --import tsx/esm --test tests/unit/preflight.test.ts` | **PASS** — 11 tests, 0 failed, 0 skipped. Final run after the test typing correction passed. No dotenv loading or provider calls. |
| `node ./node_modules/typescript/bin/tsc --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext --allowImportingTsExtensions --skipLibCheck --types node scripts/preflight.ts tests/unit/preflight.test.ts` | **PASS** — focused script/test compilation, exit 0. Initial run caught a literal-array narrowing error in an assertion; adding `as const` to that test's name list fixed it. |
| `node ./node_modules/eslint/bin/eslint.js scripts/preflight.ts tests/unit/preflight.test.ts` | **PASS** — exit 0. |
| `python3 scripts/check_docs.py` | **PASS** before concurrent A-10 report arrival: 24 required files, 51 Markdown documents. **FAIL** on final shared-checkout run: six absolute `file.ts:line` links in `docs/reviews/astra-10.md` are interpreted as missing file paths. No errors were reported for A-07's owned documents/report. This is document structure evidence only. |
| `git diff --check -- scripts/preflight.ts tests/unit/preflight.test.ts docs/ONBOARDING.md docs/INFRASTRUCTURE.md docs/INTEGRATIONS.md docs/reviews/astra-07.md` | **PASS** — scoped tracked-file whitespace check; the newly added report is also covered by the documentation checker. |
| `npm run preflight` | **PASS** — local current scope: 10/10 core names configured, 3/3 optional names configured, exit 0. Printed no values. Does not establish provider readiness. |
| `npm run preflight -- --strict` | **FAIL — expected configuration gap** — local full checklist: 10/16 core and 3/12 optional names configured, exit 1. Correct behavior; missing release configuration does not change the default development gate. |
| Full project build/typecheck/lint/test suites, database/integration tests, deployments and live provider acceptance | **NOT RUN** — owned by the main coordinator or outside this assignment. |

The strict run reported these names only: `TRIGGER_PROJECT_REF`, `GITHUB_APP_LOGIN`, `OPENROUTER_MODEL`, `CRON_SECRET`, `SIGNAL_DASHBOARD_TENANT_ID`, `SIGNAL_PILOT_CONFIG_PATH`. These are this local process's configuration gaps, not a claim about Vercel or Trigger remote secret stores. Strict is an operator checklist combining local setup and runtime requirements; do not distribute migration, CLI or provider secrets across runtimes to make one runtime pass it.

## Source findings and coordinator handoff

1. **Correct callback topology:** Slack events at `https://www.sgn.lol/api/slack/events`, Slack interactivity at `https://www.sgn.lol/api/slack/interactions`, GitHub App webhook at `https://www.sgn.lol/api/github/webhooks`. [The GitHub classifier](../../src/adapters/github-webhook.ts) requires `installation.id`; a standalone repository webhook is not a replacement. The docs now specify App registration and selected-repository installation. Passive GitHub setup uses Pull requests read plus Metadata read; Issues write is a separate action-stage grant.
2. **Actual server mappings:** [pilot validation](../../src/config/pilot.ts) and [import](../../scripts/import-pilot.ts) require a channel plus at least one named approver even for initial passive setup, accept explicit identities, and import active tenant/channel/repository/installation records. A JSON file and dashboard env ID do not establish these runtime mappings. The importer assigns `shared=false` without a provider query; the operator must verify the source is non-shared first. It validates structure/duplicates, not actual provider identities or access. No live mapping was inspected or imported by A-07.
3. **Accepted-but-unused pilot fields:** `workspaceTimezone` and `auth0Subjects` are accepted by the parser but are not persisted by the current importer. Documentation now explicitly says neither relative-deadline resolution nor Auth0 subject authorization follows from those inputs. Optional inspector remains dependent on implementation and acceptance; shared schema/import changes were not made here.
4. **Unused reader setting and metadata boundary:** [current Trigger tasks](../../src/trigger/tasks.ts) pass `SLACK_BOT_TOKEN` to the standalone replies client. They do not consume `SLACK_THREAD_READ_TOKEN` or call the newer reader's `conversations.info` validation. The docs do not present the reserved env name as a fix, and metadata read scopes are conditional on using that API. Any change to reader wiring/runtime shared-channel verification belongs to the coordinator.
5. **Project/probe truth:** [trigger.config.ts](../../trigger.config.ts) pins project `proj_cehggonaqopuuhibihif` and Node 24 directly, with no Vercel env synchronization. Setting `TRIGGER_PROJECT_REF` alone does not select a project. Current tasks export `signal.observe-event` and business/recovery tasks, not `signal.preflight` or `signal.search-public-docs`. The docs no longer direct an operator to invoke a nonexistent probe or count worker readiness as a deployed run.
6. **OpenRouter parameter-routing gap — RESOLVED in authorized follow-up:** [the adapter](../../src/adapters/openrouter.ts) now sends `provider.require_parameters: true` with strict JSON-schema `response_format`. The user explicitly extended A-07 ownership to this adapter and its focused test. See [routing correction evidence](#routing-correction-follow-up); live account/model acceptance remains NOT RUN.
7. **Preflight limits:** checks recognize explicit placeholder forms only; they do not parse URLs/PEM keys, validate optional enable flags/model selection, open the pilot path, check DB records, verify provider permissions or prove live integration. Blank supplied core names remain in scope and fail; unset future names are deferred. Configured optional names are never counted as demonstrated integration use.
8. **Concurrent documentation-check failure outside A-07 ownership:** the initial handoff's last full docs check reported only six links in A-10's newly added report: `analyze.ts:256`, `analyze.ts:286`, `page.tsx:7`, `monitoring-data.ts:25`, `monitoring.ts:122`, and `monitoring.ts:166`. The checker treats the line suffix as part of the filesystem name. A-10/coordinator can convert those report links to repository-relative file links and leave line numbers in labels. A-07 did not edit the other worker's report or the shared checker.

## Official documentation checked

- Slack [public message events](https://docs.slack.dev/reference/events/message.channels/), [private message events](https://docs.slack.dev/reference/events/message.groups), [channel info scopes](https://docs.slack.dev/reference/methods/conversations.info/) and [thread replies](https://docs.slack.dev/reference/methods/conversations.replies/): event/scopes, permitted bot reads and current rate/pagination requirements.
- Slack [mentions](https://docs.slack.dev/reference/events/app_mention/) and [posting](https://docs.slack.dev/reference/methods/chat.postMessage/): separate action setup scopes. Existing canonical interaction callback contract and implemented route supplied endpoint verification.
- GitHub [PR webhook permissions/payloads](https://docs.github.com/en/webhooks/webhook-events-and-payloads#pull_request), [App permissions](https://docs.github.com/en/apps/creating-github-apps/registering-a-github-app/choosing-permissions-for-a-github-app), [issue creation](https://docs.github.com/en/rest/issues/issues#create-an-issue), and [comments](https://docs.github.com/en/rest/issues/comments#create-an-issue-comment): App installation binding, PR read and separate Issues write permissions.
- Trigger [environment variables](https://trigger.dev/docs/deploy-environment-variables) and [deployed task authentication](https://trigger.dev/docs/deployment/overview): deployed task secrets and selected-environment enqueue credentials are separate from Vercel deployment/local env loading.
- OpenRouter [structured-output routing](https://openrouter.ai/docs/guides/features/structured-outputs): parameter-required provider routing, independently of a configured model string.
- Installed Next.js 16.3.5 guide `node_modules/next/dist/docs/01-app/02-guides/environment-variables.md`: read in full; Next dotenv precedence differs from this CLI's explicit two-file Node loading. No Next application code was changed.

The coordinator can accept the owned changes and record their focused verification in `tasks.md`, while retaining the existing live blockers and assigning shared-file corrections. No release-complete or live-integration claim is made by this report.

## Routing correction follow-up

2026-09-13, verified at 16:34 UTC. The user accepted the initial A-07 handoff and explicitly added `src/adapters/openrouter.ts` and `tests/openrouter.test.ts` ownership for this bounded correction. The same worker resumed; no agents were spawned and the branch remains `codex/signal-track-rob`.

**PASS — RED/GREEN source correction.** This follow-up changes only [src/adapters/openrouter.ts](../../src/adapters/openrouter.ts), [tests/openrouter.test.ts](../../tests/openrouter.test.ts), [docs/INTEGRATIONS.md](../INTEGRATIONS.md), and this report. The adapter change is one request-body property: `provider: { require_parameters: true }`. No provider ordering, provider allowlist, model fallback, endpoint, model choice or dependency was added or changed. Existing strict schema serialization, input guard, timeout, error sanitization, redirect rejection and 2,000-token output cap remain unchanged.

The synthetic request test now asserts the exact provider preference, unchanged messages/model/schema/output cap and redirect policy. A synthetic routing-error test covers HTTP 400, 404, 503 and a 200 error envelope with a completion: each rejects, hides provider details and makes exactly one fetch with required parameters. The existing configured input-cutoff test remains; an additional default-boundary test admits a combined 48,000-character system/user input and rejects one extra character before another fetch. This preserves the existing character-based guard; it is not new evidence of exact 12,000-token accounting. No token-budget logic was changed. Removed one pre-existing duplicate identical `model` property from the test fixture without changing its value.

The adapter uses the Node 24 Fetch/Request API directly. Installed versions inspected: Node 24.20.0, TypeScript 5.9.3 and tsx 4.23.13; installed OpenAI 7.15.0 is not used by this wire-format path. The setting's placement and behavior were checked against the current official [OpenRouter structured-output guide](https://openrouter.ai/docs/guides/features/structured-outputs) and [provider-routing guide](https://openrouter.ai/docs/guides/routing/provider-selection). Compatible-provider selection remains OpenRouter's existing policy; the client does not relax the parameter requirement when that routing fails.

All checks used `export PATH=/Users/robcr/.nvm/versions/node/v24.20.0/bin:$PATH` and existing dependencies, without dotenv loading:

| Command | Result |
|---|---|
| `node --import tsx/esm --test --test-name-pattern='sends strict JSON schema' tests/openrouter.test.ts` before source correction | **FAIL / RED as intended** — 1 test failed: actual `payload.provider` was `undefined`, expected `{ require_parameters: true }`. |
| `node --import tsx/esm --test tests/openrouter.test.ts` after source correction | **PASS / GREEN** — 6 tests, 0 failed, 0 skipped. All fetches are synthetic; no provider request was sent. |
| `node ./node_modules/typescript/bin/tsc --noEmit --strict --target ES2022 --module ESNext --moduleResolution Bundler --allowImportingTsExtensions --skipLibCheck --types node src/adapters/openrouter.ts tests/openrouter.test.ts` | **PASS** — focused adapter/test typecheck, exit 0. |
| `node ./node_modules/eslint/bin/eslint.js src/adapters/openrouter.ts tests/openrouter.test.ts` | **PASS** — exit 0. |
| `git diff --check -- src/adapters/openrouter.ts tests/openrouter.test.ts` | **PASS** — exit 0. |
| `python3 scripts/check_docs.py` after the documentation correction | **PASS** — 24 required files, 52 Markdown documents; the earlier concurrent A-10 link failures are no longer reported. A-07 did not edit that report. |
| `git diff --check -- src/adapters/openrouter.ts tests/openrouter.test.ts docs/INTEGRATIONS.md docs/reviews/astra-07.md` | **PASS** — exit 0 after source/test/documentation changes. |

**NOT RUN:** full project suites/build, exact token-count evaluation, live model/provider eligibility, account configuration and deployed acceptance. No environment values were read, no env or provider state changed, and no package/shared-contract/ledger/DB/Trigger file was edited. The integration guide's routing-gap wording was corrected only after GREEN verification.
