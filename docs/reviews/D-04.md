# D-04 review

Status: PASS for documentation scope; live integrations: NOT RUN.

The integration guide maps all eight requested tools to bounded roles: the
selected OpenAI model capability through one OpenRouter inference route,
Trigger.dev tasks, and optional Auth0, CopilotKit, Exa, Ambiguous AI, and
Mozilla.ai usage. Vercel is listed separately as supporting hosting. It records exact categories of configuration prerequisites,
timeboxes, deferral behavior, privacy boundaries, acceptance checks, and the
four-hour implementation plus two-hour protected finishing window.

Source observations included are limited to those verified during planning:
OpenRouter exposes `openai/gpt-5.6-luna` with structured-output support and
requires `require_parameters: true` for that routing behavior; Trigger task
idempotency supplements the database operation guard; Auth0 uses the
`auth0/client` marketplace entry and current Next.js Auth0Client guidance;
`exa/exa-search-api` is available; Mozilla.ai documents `LlmJudge.run`; and
Ambiguous AI exposes the confirmed documents GET endpoint. Unsupported
pagination, filtering, per-document, or authority claims are deliberately
left as prerequisites.

Self-check: all links in `docs/PRODUCT.md` resolve to this file; business task
arguments and scheduled-task metadata follow `docs/CONTRACTS.md`; absent
reconciliation markers explicitly prohibit another POST. No production
code, dependency, lockfile, task ledger, credentials, or external service was
changed. Initial live evidence remains `NOT RUN` except Vercel CLI access,
which is explicitly only `PARTIAL` evidence for hosting.
