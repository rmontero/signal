# D-03 review

Authored the bounded verification, operations, and demo package from `north_star.md`, `roadmap.md`, and `docs/PRODUCT.md`.

Self-check: MVP-01 through MVP-15 are mapped to concrete scenarios with expected results and initial `NOT RUN` status. The matrix includes duplicate delivery/click/task-start checks, callback forgery, expired approval, tenant boundaries, source overflow, injection and malformed AI output, missing identity, both uncertain GitHub mutations, crash dispatch gaps, Slack-only retries, dropped assignee responses, and active sending leases. The fixture corpus has 20 categories and a 90% extraction threshold. Future npm commands are explicitly `NOT IMPLEMENTED` until implementation exists. Operations covers no-blind-retry reconciliation, manual resolution, revocation, stale outbox/lease handling, logging and retention, optional disablement, rollback, and exact sanitized Exa approval. The demo is under two minutes, uses synthetic Carlos/Ana data, and requires create, comment, and replay evidence.

Correction pass checked the final `docs/CONTRACTS.md`. Lease recovery now explicitly requires an expired lease plus a confirmed terminal owning Trigger attempt/run with no continuing retry; an owner that is still running remains blocked. The operations guidance also states that an absent marker never authorizes resending a POST. The demo includes copy-pasteable Carlos/Ana source and an exact `@Signal update #<actual-number>` follow-up pattern. Live tests remain `NOT RUN` until implementation and credentials exist.
