# D-02 architecture contract delivery

Owner: Astra architect. Started 2026-09-11 18:26 America/Mexico_City. Status: **REVIEW** pending coordinator/integration review. Documentation only; implementation clock not started by this task.

## Owned deliverables

- [ARCHITECTURE.md](../ARCHITECTURE.md): runtime boundaries, evidence pipeline, approvals, retention and optional surfaces.
- [CONTRACTS.md](../CONTRACTS.md): TypeScript payloads, compound persistence constraints, dispatch and operation transitions, route behavior, concurrent mutation/reconciliation fences and separate Exa consent.
- [INFRASTRUCTURE.md](../INFRASTRUCTURE.md): exact inputs, pilot configuration shape, both Vercel/Trigger deployment environments and real capability preflight gates.
- [SAAS.md](../SAAS.md): explicit post-six-hour expansion and second-tenant admission gate.

Combined main-document length: 3,347 words at verification. Only these four documents and this report were edited by this worker. No code, installs, provisioning, service accounts, external messages, commits or deployments were performed.

## Decisions integrated

The coordinator retained operation states `READY`, `SENDING`, `SUCCEEDED`, `FAILED`, `UNKNOWN`, `STALE`, `/healthz`, and protected `/api/internal/maintenance`. Task arguments are `{tenantId,jobId,schemaVersion:1}`. The outbox row is the durable job; there is no separate jobs table. `DATABASE_URL_UNPOOLED` and `CRON_SECRET` match the coordinator's configuration template. Auth0 owns `/auth/*` through its current SDK.

An expired mutation lease alone is insufficient: the owning attempt/run must be confirmed terminal before reconciliation. Missing markers never authorize reposting. Exa consent binds an exact sanitized query independently of GitHub approval.

## Verification and limitations

- **PASS** — `python3 scripts/check_docs.py`: 21 required files, local links, titles, requirement/partner coverage and environment names. Structural documentation evidence only.
- **PASS** — scoped Python local-link/fenced-block check across the four documents; `git diff --check` reported no whitespace errors.
- **PASS** — primary source review for Trigger idempotency/tasks, Slack replies/interactivity, GitHub issue/comment APIs, OpenRouter structured output, and Auth0 Next.js. Neon documentation fetched with HTTP 200 via `curl`; its direct/unpooled variable name was verified. Official links are embedded beside relevant assumptions.
- **NOT RUN** — application build, unit/database tests, credentials, installations, deployments and real integration smoke tests.

Coordinator follow-ups: ensure operations and verification explicitly test expired lease with still-running owner; avoid language suggesting reconciliation can lead to a blind repost. The source snapshot and provider-retention requirements still need implementation and operational verification; documentation does not prove cleanup or provider retention settings.

Next ready dependency: D-05 cross-document review after the coordinator integrates all documentation reports. This report is delivery evidence, not a competing task ledger.
